@model IEnumerable<rentCar.Models.MODELO>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/dashboardLayout.cshtml";
}

<h2>Lista de modelos</h2>

<p>
    @Html.ActionLink("Crear nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NOMBRE_MODELO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ESTADO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MARCA.NOMBRE_MARCA)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NOMBRE_MODELO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ESTADO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MARCA.NOMBRE_MARCA)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.ID_MODELO }) |
            @Html.ActionLink("Detalle", "Details", new { id=item.ID_MODELO }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.ID_MODELO })
        </td>
    </tr>
}

</table>
