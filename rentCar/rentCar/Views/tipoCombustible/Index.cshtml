@model IEnumerable<rentCar.Models.TIPO_COMBUSTIBLE>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/dashboardLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NOMBRE_TIPO_COMBUSTIBLE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ESTADO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NOMBRE_TIPO_COMBUSTIBLE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ESTADO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_TIPO_COMBUSTIBLE }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_TIPO_COMBUSTIBLE }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_TIPO_COMBUSTIBLE })
        </td>
    </tr>
}

</table>
